## backend/app/services/stt.py (stub)

from ..core.config import OPENAI_TRANSCRIBE_MODEL

async def transcribe(audio_url: str) -> str:
    # TODO: pull file, run Whisper, return text
    return "Transcription française simulée."

